networks:
  chain-portal-network:
    driver: bridge

services:
  angular-client:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:80"
    restart: always
    networks:
      - chain-portal-network
